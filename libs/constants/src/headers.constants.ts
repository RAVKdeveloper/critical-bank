export enum CookieAuthKeys {
  CRITICAL_ACCESS_TOKEN = 'critical_access_token',
  CRITICAL_REFRESH_TOKEN = 'critical_refresh_token',
}

export enum RequestHeaders {
  USER = 'user',
  TG_USER = 'tg-user',
}
