/* eslint-disable */

// @ts-ignore
import { getClient } from './db'

export const up = async () => {
  const client = await getClient()
}

export const down = async () => {
  const client = await getClient()
}
